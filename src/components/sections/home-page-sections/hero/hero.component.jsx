import React from 'react';

import './hero.styles.scss';
import NavBar from '../../../nav-bar/nav-bar.component';

const Hero = () => {
  return (
    <section id='hero'>
      <NavBar />
    </section>
  );
};

export default Hero;
